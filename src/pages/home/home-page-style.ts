import styled from 'styled-components';

const HomePage = styled.div`
  font-family: ${({ theme }) => theme.font.imcre};
  font-size: 5.2rem;
  color: ${({ theme }) => theme.color.gray_BDBDBD};
`;

export { HomePage };
