import styled from 'styled-components';

const SignupPage = styled.div`
  font-size: 5.2rem;
  font-weight: 700;
  color: ${({ theme }) => theme.color.gray_BDBDBD};
`;

export { SignupPage };
